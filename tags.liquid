---
pagination:
  data: collections
  size: 1
  alias: tag
  filter:
    - all
    - nav
    - page
    - book
layout: layout.liquid
renderData:
  title: Tagged “{{ tag }}”
permalink: /tags/{{ tag }}/
---
<h1>Tagged “{{ tag }}”</h1>

{% set pagelist = collections[ tag ] %}
{% include "pagelist.liquid" %}

<p>See <a href="{{ '/tags/' }}">all tags</a>.</p>
