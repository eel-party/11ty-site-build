---
layout: layout.liquid
title: page layout
---
<!-- responsive not working in chrome -->
<!-- tachyons "-m" is important -->
<section>
    {{ content }}
    <article class="mw9 center ph0-ns">
        <div class="cf ph0-ns">
            <div class="fl w-100 w-50-ns pr3-m">
                <div class="bt bw2 pv1">
                    <h2>
                        {{ title }}
                    </h2>
                    <p>{{ description }}</p>

                    <!-- doing this wrong -->
                    <!-- <ul>
                        {% for page in collections.page %}
                        <li>{{ page.data.tags }}</li>
                        {% endfor %}
                    </ul> -->
                    <!-- not showing up, possibly due to lack of pages -->
                    <!-- {% for book in collections.book %}
                    {% if book.data.title == title %}
                    <a href="{{ book.url }}" class="dim near-black">
                        <p>&#8592; back to {{ book.data.title }}</p>
                    </a>
                    {% endif %}
                    {% endfor %} -->
                </div>
            </div>
            <div class="fl w-100 w-50-ns">
                <div class="pv0">
                    <img src="../_img/{{ image }}" alt="{{ description }}" />
                </div>
            </div>
        </div>
    </article>
</section>