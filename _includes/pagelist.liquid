---
layout: layout.liquid
title: page list for each book
---
{{content}}
<p>Filter this list by book:</p>
<ul>
    {% for page in collections.page %}
    {% if page.data.title == title %}
    
    <li><img src="{{ page.url }}/{{ page.data.image }}" style="max-height: 100px;" /><a href="{{ page.url }}">{{ page.data.title }} - {{ page.data.description }} </a></li>
    {% endif %}
    {% endfor %}
</ul>

